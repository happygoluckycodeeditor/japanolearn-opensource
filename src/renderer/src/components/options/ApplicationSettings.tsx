import React from 'react'

const ApplicationSettings: React.FC = () => {
  return <p className="text-sm">Application settings will be available in future updates.</p>
}

export default ApplicationSettings
